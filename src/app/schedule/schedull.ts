export class Schedule {
  // environment: string;
  trackerId: [];
  // status: string;
  constructor(tracking: []) {
    // this.environment = env;
    this.trackerId = tracking;
    // this.status = status;
  }
}
